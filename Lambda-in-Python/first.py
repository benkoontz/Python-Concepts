# Lambda is a small function that has no name
# They don't have a name becuase they're used one time
# Functions are used when you need to use code over and over again


answer = lambda x: x*7 # pass in a number and multiply it by 7
print(answer(5))
